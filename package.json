{
  "name": "avrupa-gazetesi",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "seo-check": "node scripts/seo-check.mjs",
    "audit:editorial": "node scripts/editorial-audit.mjs",
    "audit:sources": "node scripts/check-source-links.mjs",
    "content:links": "node scripts/add-contextual-links.mjs",
    "content:photos": "node scripts/assign-editorial-photo-bank.mjs",
    "test": "npm run check && npm run audit:editorial && npm run build && npm run seo-check"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.2"
  }
}
